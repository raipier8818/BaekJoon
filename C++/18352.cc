#include <bits/stdc++.h>

using namespace std;

int n,m,k,x;
int a,b;

int city[300001];


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m >> k >> x;

    for(int i = 0; i < m; i++){
        cin >> a >> b;
        
    }
}