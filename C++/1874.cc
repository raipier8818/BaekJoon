#include <bits/stdc++.h>
using namespace std;

int n;
queue <int> q;
vector <int> v;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    for(int i = 0; i < n; i++){
        int num;
        cin >> num;
        v.push_back(num);
    }

    while (q.size() != n)
    {
        int check = 1;
        int i = check;
        for(i = check; i <= n; i++){
            if(v[check] == i){
                
            }
        }
    }
    



}