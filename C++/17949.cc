#include <bits/stdc++.h>
using namespace std;

string A;
int n;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> A;
    cin >> n;

    for(int i = 0; i < n; i++){
        string comp;
        cin >> comp;
        if(comp == "int"){
            
        }
    }
}