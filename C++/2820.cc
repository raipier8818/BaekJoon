#include <bits/stdc++.h>

using namespace std;
using ll = long long;
int n,m,size;
ll tree[4*500000];
ll lazy[4*500000];

void init(int pos, ll val){
    int index = pos + size - 1;

}



int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;
    
    for(size = 1; size < n; size *= 2);
    
    
    for(int i = 0; i < n; i++){
        ll a,b;

        cin >> a >> b;



    }
}