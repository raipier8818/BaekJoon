#include <bits/stdc++.h>
using namespace std;

int n;
vector<string> a;

char arr[100];

int find(string a, string b){
    if(a.size() > b.size()){
        
    }
}



int main(){
    cin >> n;
    for(int i = 0; i < n; i++){
        string r;
        cin >> r;
        a.push_back(r);
    }

    
}