#include <bits/stdc++.h>

using namespace std;

int n,c;
vector<int> pos;

int main(){
    cin >> n >> c;
    for(int i = 0; i < n; i++){
        int p;
        cin >> p;
        pos.push_back(p);
    }
    sort(pos.begin(), pos.end());
    
    
}