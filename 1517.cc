#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> arr;

void mergesort(int start, int end){
    if(start >= end) return;

    vector<int> left;
    vector<int> right;

    
}



int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    arr.resize(n);
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }
}